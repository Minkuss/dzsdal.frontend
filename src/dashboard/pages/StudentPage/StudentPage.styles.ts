import { style } from "typestyle";
import { px } from "csx";

export const subjectSelector = style({
  marginTop: px(52),
});

export const homeWorkList = style({
  marginTop: px(30),
});

export const comments = style({
  marginTop: px(20),
});

export const icon = style({
  marginTop: px(50),
  textAlign: "center",
  height: px(400),
});
